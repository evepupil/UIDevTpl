# UIDevTpl V1 产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v1.2-draft |
| 文档状态 | 需求草案 |
| 最近更新 | 2026-08-10 |
| 产品名称 | UIDevTpl |
| 一句话定位 | 为 AI 编程提供可预览、可下载、可迁移的精选 UI 开发模板 |
| V1 产品形态 | 模板网站 + 复制 AI 模板迁移指令 |
| 首个模板实现 | Web / React / HeroUI |
| 部署目标 | Cloudflare Workers |

## 1. 文档边界

本文是 UIDevTpl V1 的产品需求基准，只回答以下问题：

- 用户在网站上能看到什么、操作什么。
- 模板如何预览、选择和分享。
- 用户复制给 AI 的迁移指令需要完成什么结果。
- V1 交付哪些能力，如何判断完成。
- 已经讨论并确认了哪些产品决策。

模板源码的分层、React/Vue 实现方式、状态矩阵和发布质量要求单独记录在 [UIDevTpl 模板规范 v1](./模板规范-v1.md)。技术架构、仓库组织、构建流水线和未来工具集成另写技术设计，不混入本文。

## 2. 背景与问题

用户用 AI 编程工具制作网站时，通常会说“做一个 SaaS 后台”“做一个宠物网站”或“要高级、现代一些”。这类描述缺少明确的颜色、字体、间距、组件状态、布局节奏、图片规则和响应式约束，AI 很容易生成相似且普通的页面。

常见问题包括：

- 页面大量堆叠同质化卡片、渐变和超大标题。
- 颜色、圆角、字体、阴影和间距缺少统一规则。
- 首个页面勉强可用，后续页面逐渐偏离原有风格。
- 只复制一段风格提示词时，不同 AI、不同轮次的理解差异很大。
- 用户为了复刻审美，需要反复发送截图和修正细节。
- 组件库只提供基础能力，没有替用户选好完整的视觉语言和页面构图。

UIDevTpl 通过人工打磨的真实模板源码解决这个问题。用户先在网站查看真实效果，再复制一段“AI 模板迁移指令”。AI 按指令下载固定版本模板包，把原始代码保留在项目的 `.uidevtpl/` 中，然后将需要的组件、tokens、字体、布局和页面骨架迁移到用户项目。

## 3. 已确认决策

以下决策已经确认，后续设计和实现默认遵守。如需调整，应先更新本文并记录原因。

| 编号 | 决策 | 结论 |
| --- | --- | --- |
| D-001 | 产品名称 | 品牌使用 `UIDevTpl`，仓库与命令使用 `uidevtpl`，项目参考目录使用 `.uidevtpl/` |
| D-002 | 核心复刻方式 | 依赖真实模板代码包，简单视觉形容词提示词不作为模板复刻方案 |
| D-003 | V1 产品形态 | 只交付模板网站和“复制给 AI”的模板迁移指令 |
| D-004 | V1 工具边界 | MCP、官方 CLI、IDE 插件和 AI 工具适配器均不进入 V1 |
| D-005 | 提示词职责 | 提示词要求 AI 下载、校验、解压、阅读并迁移固定版本模板源码 |
| D-006 | 本地保留方式 | 原始模板长期保存在 `.uidevtpl/<platform>/<framework>/<library>/<template>/` |
| D-007 | React/Vue 策略 | 视觉家族共享审美规则，React 与 Vue 维护独立真实源码和版本 |
| D-008 | 模板 ID | 使用 `<platform>/<framework>/<component-library>/<template>`，例如 `web/react/heroui/quiet-grid` |
| D-009 | 首个实现 | V1 首个实现使用 React 与 HeroUI，Vue 实现后续单独发布 |
| D-010 | 模板与场景关系 | 组件库、视觉家族和应用场景分开；场景用于展示和筛选，不限制模板用途 |
| D-011 | 网站体验参考 | 参考 v0 by Vercel 的开发工具感、模板浏览效率和预览体验，品牌与界面独立设计 |
| D-012 | 预览方式 | 同时提供详情页内嵌真实交互预览和新标签页独立预览 |
| D-013 | 分辨率检查 | 内嵌预览提供常用视口；独立预览直接响应浏览器窗口和设备模拟尺寸 |
| D-014 | 模板代码要求 | 组件按原子、组合、布局、页面骨架和 Showcase 分层，业务内容与基础组件解耦 |
| D-015 | 脚手架边界 | 产品不生成完整项目，不负责一键安装全部依赖，也不自动启动开发服务器 |
| D-016 | 部署方式 | 网站部署在 Cloudflare Workers |
| D-017 | 版本原则 | 网站预览、下载包、迁移指令和参考图必须指向同一个固定模板版本 |
| D-018 | 平台与运行宿主 | `platform` 进入模板 ID；浏览器、Electron、Tauri、Expo 等 `runtime` 写入兼容信息 |
| D-019 | V1 发布规模 | 一个完整视觉家族通过内部闭环，公开 V1 至少发布两个明显不同的视觉家族 |
| D-020 | 首个模板主题与语言 | `quiet-grid` 的 V1 实现只发布 Light；网站界面支持 Light / Dark 与中文 / English 切换，模板自身支持的主题单独声明 |
| D-021 | 模板许可证 | 模板代码使用 MIT；字体、图片和图标逐项记录许可证；UIDevTpl 名称、Logo 和品牌资产不随模板代码授权 |
| D-022 | 客户端与移动端 | V1 只交付 Web 模板；桌面端与移动端保留平台模型，具体模板在 V1 后评估 |
| D-023 | 紧急下架 | 已发布内容保持不可变；安全或许可证事故可将发布记录的 `availability` 改为 `withdrawn` 并关闭下载 |
| D-024 | 首页浏览结构 | 首页参考 v0 Templates 的信息层级：紧凑导航、标题与搜索、分类预览、精选模板和可组合筛选；保留 UIDevTpl 品牌与自有视觉 |
| D-025 | 分类体系 | 提供 Category 导航、首页分类预览、分类总览与单分类结果；分类属于目录层数据，可以扩展且不改变模板身份 |
| D-026 | 登录入口 | 顶部保留“登录”和“注册”；当前原型不连接账号系统，生产能力通过开关开放，关闭时入口展示明确的暂未开放反馈 |
| D-027 | 分享者身份 | 每个模板目录条目展示分享者名称与头像，并区分官方、合作作者和社区用户，为用户分享模板保留统一字段 |
| D-028 | 点赞 | 模板卡片和详情提供心形点赞入口与总数；原型支持本地切换，生产持久化、去重与防滥用随账号或匿名会话能力落地 |
| D-029 | 用户分享预留 | 目录模型预留投稿来源、作者、可见性、审核状态和分享时间；当前原型不接收真实投稿，后续开放时沿用同一目录条目 |
| D-030 | 首版扩展原则 | 已确认的后续能力必须进入需求、数据契约和页面位置；可以通过能力开关暂时关闭，禁止因首版未开放而删掉必要结构 |

## 4. 产品定位

UIDevTpl 是面向 AI 编程用户和前端开发者的精选 UI 开发模板网站。每套公开模板都包含真实组件源码、Design Tokens、字体规则、布局组件、页面骨架、交互状态、演示页面和参考资产。

产品服务两个连续动作：

1. 选风格：用户浏览、筛选并真实操作模板，确认审美和技术栈。
2. 用风格：用户把迁移指令交给当前项目中的 AI，AI 下载固定版本源码并迁移到项目。

UIDevTpl 的价值来自模板本身的完成度和复刻稳定性。网站展示、下载产物和 AI 最终使用的代码需要保持一致。

## 5. 产品原则

### 5.1 真实代码驱动

公开使用入口必须让 AI 获取真实模板包。只描述颜色、字体和氛围的文字可以作为模板介绍，不能成为复刻主流程。

### 5.2 审美质量优先

每套公开模板需要人工打磨和视觉验收。V1 可以少，不能用浅层换色和低完成度变体填充数量。

### 5.3 先看效果再使用

用户在复制迁移指令前，应能查看完整页面、组件状态、桌面和手机效果，并实际操作主要交互。

### 5.4 固定版本可复现

用户复制迁移指令时，系统必须把当前选择解析为固定实现版本。已经发布的版本、下载地址和预览内容不能静默改变。

### 5.5 尊重现有项目

迁移指令要求 AI 先读项目结构和规则，再决定文件位置、依赖与接入方式。模板目录只提供参考源，业务代码进入用户项目现有结构。

### 5.6 能力边界清楚

网站没有用户本地项目权限。V1 的“快速使用”通过复制迁移指令完成，后续是否增加 CLI、MCP 或 IDE 集成单独评估。

## 6. V1 目标与成功标准

### 6.1 产品目标

1. 用户能按框架、组件库和视觉特征找到合适模板。
2. 用户能在详情页内真实操作模板，并检查常见分辨率。
3. 用户能在独立浏览器标签页检查完整页面和任意视口。
4. 用户能一次复制完整的 AI 模板迁移指令。
5. AI 能依据指令下载固定模板包并保存到 `.uidevtpl/`。
6. AI 能基于真实源码把风格迁移到用户现有项目。
7. 后续新增页面时，AI 能持续参考同一模板目录保持风格一致。
8. 内容维护者能持续增加视觉家族、框架实现和 Showcase。
9. 用户能从首页和分类页按用途浏览模板，并看清分享者与社区热度。
10. 网站偏好、账号入口和未来投稿入口拥有稳定位置与数据契约。

### 6.2 成功指标

| 指标 | V1 目标 |
| --- | --- |
| 从模板详情到复制迁移指令 | 不超过 2 次主要操作 |
| 从内嵌预览到独立预览 | 1 次主要操作 |
| 核心交互可操作率 | 模板声明支持的交互 100% 可操作 |
| 固定版本一致性 | 预览、模板包、迁移指令和版本信息 100% 一致 |
| 响应式验收 | 360px、768px、1024px、1440px 无阻断级溢出或遮挡 |
| 受控项目迁移成功率 | 10 个受控任务至少成功 9 个 |
| AI 风格延续评分 | 三个不同题材的人工评分平均不低于 4/5，且无单项低于 3/5 |
| 公开模板阻断级问题 | 0 个 |

“受控项目迁移成功”指 AI 完成模板下载、校验、`.uidevtpl` 落盘、源码迁移和项目原有门禁。首批任务由 Codex 和 Claude Code 各执行 5 个；因目标项目本身损坏或用户拒绝必要依赖变更导致的失败单独记录。任何静默覆盖、跳过校验或伪造验证结果的行为都属于发布阻断问题，不能被总体成功率抵消。

## 7. V1 范围

### 7.1 V1 必须包含

- 模板列表、搜索、筛选和排序。
- 首页分类预览、分类总览与单分类结果。
- 按平台、框架与组件库识别模板实现。
- 模板卡片显示分享者名称、头像、浏览量和点赞数。
- 心形点赞支持即时切换；原型使用本地状态，生产持久化由能力开关控制。
- 网站 Light / Dark 主题与中文 / English 切换。
- 顶部登录、注册入口，以及关闭账号能力时的可理解反馈。
- 为未来用户分享预留目录条目、审核状态和页面入口。
- 模板详情页和固定版本页面。
- 一个 React / HeroUI 的完整模板实现通过内部闭环。
- 公开发布时至少包含两个明显不同的视觉家族，每个家族至少有一个完整 Web 实现。
- 组件实验室和至少两个不同题材的 Showcase。
- 详情页内嵌真实交互预览。
- 桌面、平板、手机快捷视口。
- 新标签页独立预览。
- 固定版本模板包下载地址和校验值。
- 一键复制 AI 模板迁移指令。
- `.uidevtpl/<platform>/<framework>/<library>/<template>/` 本地保留约定。
- 迁移失败时的明确停止与报告要求。
- Cloudflare Workers 生产部署。

### 7.2 V1 明确不做

- MCP 服务。
- 官方 CLI。
- Cursor、Codex、Claude Code 等工具的专用适配器或插件。
- 网站直接读取或修改用户本地项目。
- 完整项目脚手架生成。
- 网站自动安装依赖或启动开发服务器。
- 在线拖拽编辑器。
- 账号鉴权、个人中心和账号跨设备同步。
- 用户投稿接收、审核后台、评论、评分、团队模板和私人模板。
- 依赖账号的跨设备点赞历史；V1 保留交互与数据契约。
- 在线 CMS。
- Figma 插件或设计工具导出。
- 只复制视觉形容词的简单风格提示词入口。
- React 与 Vue 源码自动互转后直接发布。
- Electron、Tauri 和 React Native 模板实现。
- 远程执行用户项目构建、测试或截图。

### 7.3 V1 之后再评估

- Vue 的首个独立模板实现。
- 更多 React 与 Vue 组件库。
- Electron、Tauri 等桌面端 UI 模板与宿主接入说明。
- React Native / Expo 等移动端模板与原生预览能力。
- 官方 CLI 与模板版本升级能力。
- AI 工具适配器和 IDE 插件。
- MCP 模板发现与分发。
- 账号系统、个人主页和跨设备点赞同步。
- 用户投稿、审核、团队模板和私有模板。

## 8. 目标用户

### 8.1 AI 编程用户

使用 Codex、Claude Code、Cursor、Windsurf、GitHub Copilot 等工具制作网站，希望减少审美沟通和返工。

核心需求：

- 快速确认一套真正好看的风格。
- 直接把真实模板源码交给 AI 参考。
- 在后续页面中持续保持一致。
- 避免依靠抽象形容词反复试错。

### 8.2 独立开发者和小团队

缺少专职设计师，需要在现有项目中快速建立可靠的组件、tokens 和页面骨架。

核心需求：

- 选择与现有框架和组件库兼容的实现。
- 看清依赖、文件内容和使用边界。
- 固定版本，避免模板更新影响现有项目。

### 8.3 模板维护者

负责设计、实现、审核和发布 UIDevTpl 模板。

核心需求：

- 按统一规范组织模板。
- 用同一份实现生成网站预览和下载包。
- 在发布前检查状态、响应式、资产授权和迁移可用性。

## 9. 核心概念

| 概念 | 说明 |
| --- | --- |
| 视觉家族 | 与框架无关的审美基线，包含 tokens、字体、图片、布局和视觉规则 |
| 平台 | 模板面向的界面环境，当前取值为 `web`，未来可增加 `desktop` 和 `mobile` |
| 运行宿主 | 模板实际运行的环境，例如 `browser`、`electron`、`tauri` 或 `expo` |
| 模板实现 | 某个视觉家族针对特定平台、框架和组件库完成的真实源码 |
| 模板 ID | `<platform>/<framework>/<library>/<template>`，例如 `web/react/heroui/quiet-grid` |
| 家族版本 | 视觉家族的固定版本，例如 `quiet-grid@1.0.0` |
| 实现版本 | 某个模板实现的固定版本，例如 `web/react/heroui/quiet-grid@1.0.0` |
| 模板包 | 包含模板真实源码、规则、说明、参考图和校验信息的固定版本压缩包 |
| 组件实验室 | 集中展示真实组件和全部适用状态的预览页面 |
| Showcase | 使用模板制作的完整演示页面 |
| Category | 面向浏览的用途分类，一个目录条目可以属于多个分类，不改变模板 ID |
| 目录条目 | 首页和分类页展示的发布单元，关联固定模板版本、Showcase、分享者与聚合统计 |
| 分享者 | 将目录条目公开到平台的官方组织、合作作者或社区用户；与模板代码版权声明分别记录 |
| 页面骨架 | 固化主要构图、内容区域和响应式关系的可复用页面结构 |
| AI 模板迁移指令 | 要求 AI 下载、校验、解压、阅读并迁移模板源码的完整任务文本 |
| `.uidevtpl` | 用户项目中长期保存原始模板参考代码的目录 |

## 10. 信息架构

### 10.1 页面与路由

| 页面 | 建议路由 | 作用 |
| --- | --- | --- |
| 首页 / 模板库 | `/`、`/templates` | 搜索、分类预览、精选模板和全部模板 |
| 分类总览 | `/categories` | 浏览全部用途分类及代表模板拼图 |
| 单分类 | `/categories/:slug` | 查看指定分类下的模板目录条目 |
| 最新稳定实现详情 | `/templates/:platform/:framework/:library/:slug` | 查看当前最新稳定版本 |
| 固定版本详情 | `/templates/:platform/:framework/:library/:slug/:version` | 查看不可变版本并复制迁移指令 |
| 独立预览 | `/preview/:platform/:framework/:library/:slug/:version/:showcase` | 在完整浏览器视口运行真实 Showcase |
| 使用说明 | `/docs/use-with-ai` | 解释复制指令、`.uidevtpl` 和迁移流程 |
| 登录 / 注册 | `/login`、`/signup` | 预留账号入口；能力关闭时展示暂未开放状态 |
| 分享模板 | `/submit` | 预留用户投稿入口；能力关闭时不接收数据 |

首屏使用紧凑产品介绍和居中搜索，不设置占满首屏的营销 Hero。首个桌面视口内必须看到分类入口或模板内容；手机端在搜索后立即进入横向分类列表和模板流。

### 10.2 导航

V1 顶部导航保持精简：

- UIDevTpl 品牌入口。
- 模板库与 Category 下拉导航。
- 使用说明。
- GitHub 或源码入口，是否展示取决于模板许可证决策。
- 中文 / English 切换。
- Light / Dark 主题切换。
- 登录和注册入口；账号能力关闭时保留入口并反馈状态。

### 10.3 模板详情信息顺序

详情页按以下顺序组织：

1. 模板名称、模板 ID、框架、组件库和固定版本。
2. 视觉标签、支持主题和兼容范围。
3. 主要操作：“复制给 AI”“在新标签页打开”。
4. 内嵌完整页面预览。
5. Showcase、视口和主题切换。
6. 组件实验室。
7. 模板包含的组件、tokens、字体、布局和页面骨架。
8. 使用要求、限制和模板包内容。
9. 固定版本、更新时间和变更说明。

## 11. 网站视觉与交互方向

### 11.1 整体气质

网站参考 v0 by Vercel Templates 的开发工具感、信息层级和浏览效率，整体应安静、清晰、克制，优先展示真实模板内容。

要求：

- 使用中性背景和清晰分割，品牌强调色保持克制。
- 导航和说明区域紧凑，避免抢占预览空间。
- 首页使用居中标题、短说明和搜索框，留白服务于检索与分类，不做营销叙事。
- Category 以四张代表缩略图组成的预览卡展示；总览页在宽屏使用三列大卡，手机端单列。
- 精选模板在宽屏使用三列或两列高质量预览，缩略图保持稳定比例，作者与热度紧邻标题。
- 模板缩略图可以使用卡片，页面区块不做层层悬浮卡片。
- 标题尺寸与工具页面匹配，避免营销页式超大文字。
- 真实 Showcase 截图和运行预览承担主要视觉表达。
- 品牌、颜色、排版、图标和页面结构独立设计，不复刻参考产品的具体实现。
- Light 与 Dark 拥有完整语义颜色；主题切换不能只反转颜色或降低预览可读性。
- 中英文切换后导航、标题、筛选、空状态和操作文案全部同步，模板名称优先使用对应语言字段。

### 11.2 操作优先级

详情页的操作优先级为：

1. 查看并操作预览。
2. 切换 Showcase、视口和主题。
3. 在新标签页打开。
4. 复制给 AI。
5. 查看模板内容和版本说明。

主要操作在桌面和手机端都应保持可见，不能遮挡预览或与页面内容重叠。

## 12. 详细功能需求

### 12.1 模板库

#### FR-001 模板列表

优先级：P0

系统必须展示所有已发布模板实现。每张模板卡片至少包含：

- 当前语言下的模板名称和视觉家族名称。
- 真实 Showcase 缩略图。
- 分享者头像和名称。
- 浏览量、点赞数和可点击心形状态。
- 所属 Category。
- 平台。
- 框架和组件库。
- 最新稳定实现版本。
- 支持的 Light、Dark 主题。
- 视觉标签，例如克制、活泼、编辑感、未来感。
- 密度、形状和对比度标签。
- Showcase 数量。
- 最近更新时间。

验收标准：

- 制作状态为 `draft`、`review`，或公开可用性为 `withdrawn` 的实现不会出现在公开列表。
- 缩略图来自该实现当前稳定版本的真实 Showcase。
- 图片加载前有稳定宽高比，不引发布局跳动。
- 360px 宽度下文字、标签和操作无溢出或遮挡。
- 同一视觉家族有多个实现时，卡片清楚显示可用框架，不混用预览图。
- 分享者名称和头像缺失时使用审核通过的默认身份，页面不能出现破图或空白署名。
- 点击心形后图标、`aria-pressed` 和数量即时一致，失败时回滚并明确提示。

#### FR-002 搜索、筛选与排序

优先级：P0

搜索范围包括：

- 模板中英文名称。
- 模板 ID。
- 视觉家族名称。
- 平台。
- 框架和组件库。
- 视觉标签和辅助场景标签。

筛选维度包括：

- 平台。
- 框架。
- 组件库。
- Light、Dark 主题。
- 视觉气质。
- 界面密度。
- 形状倾向。
- 对比度。
- 辅助应用场景。

排序包括：

- 推荐。
- 最新发布。
- 最近更新。
- 名称。

验收标准：

- 搜索忽略英文大小写和首尾空格。
- 搜索、筛选和排序可以组合使用。
- 当前条件反映在 URL 中，刷新和分享后可以恢复。
- 无结果时提供清除条件入口。
- 应用场景只作为辅助标签，不改变模板身份和可用范围。

#### FR-003 Category 浏览

优先级：P0

系统必须提供 Category 下拉导航、首页分类预览、分类总览页和单分类结果。首批至少覆盖：

- 应用与游戏。
- 营销与落地页。
- 数据看板。
- 组件与交互。
- 登录与注册。
- 内容与作品集。

验收标准：

- 每个分类有稳定 `id`、中英文名称、排序和四个代表缩略图位置。
- 分类总览在 1440px 使用三列，在 390px 使用单列且无横向溢出。
- 从顶部菜单、首页分类卡和分类总览进入同一分类时，URL 与结果一致。
- 分类为空时仍保留分类身份并显示空状态，不能静默跳回全部模板。

#### FR-004 网站偏好与账号预留

优先级：P0

- 网站提供 Light / Dark 主题按钮，选择写入本地偏好并在刷新后恢复；没有偏好时跟随系统主题。
- 网站提供中文 / English 切换，选择写入本地偏好并在刷新后恢复。
- 主题和语言属于网站外壳状态，不改变模板自身声明的 `themes`。
- 顶部展示登录与注册入口。账号能力关闭时，点击后给出暂未开放反馈，不发起伪登录请求。
- 账号、投稿和持久化点赞通过显式能力开关控制，关闭状态也必须保留稳定页面位置与路由契约。

#### FR-005 分享者、点赞与用户分享预留

优先级：P0

目录条目必须关联固定模板版本和 Showcase，并包含分享者身份、来源、可见性、审核状态及聚合指标。模板卡片与详情使用同一条目录数据。

验收标准：

- 公开条目只接受 `published` 且 `active` 的固定模板版本。
- 分享者至少包含稳定 ID、显示名称、头像、类型和公开主页预留路径。
- 分享来源区分 `official`、`partner`、`community`，页面不把“分享者”自动写成模板代码作者。
- 原型中的点赞状态只在当前浏览器本地保存；生产持久化开启后按账号或匿名会话去重并具备限流。
- 未来社区投稿沿用同一结构，新增提交、审核和下架流程时无需改写模板卡片数据模型。

### 12.2 模板详情

#### FR-010 模板身份与兼容信息

优先级：P0

详情页必须明确显示：

- 完整模板 ID，例如 `web/react/heroui/quiet-grid`。
- 固定实现版本和所属家族版本。
- 平台和运行宿主。
- 框架、组件库及其兼容范围。
- 需要的语言模式、样式系统和主要依赖。
- 包内制作状态，公开版本固定为 `published`。
- 公开可用性：`active`、`deprecated` 或 `withdrawn`。
- 发布时间、最近更新时间和变更摘要。
- 模板包含内容和不包含内容。

用户访问不带版本的详情地址时，网站解析到最新 `active` 稳定版本，并在页面和复制内容中显示解析后的固定版本。`deprecated` 和 `withdrawn` 版本不参与最新稳定版本解析。固定版本页面的内容长期保持不变；`withdrawn` 页面需要显示下架原因、时间和替代版本。

#### FR-011 模板内容清单

优先级：P0

详情页必须展示：

- 语义颜色角色和支持主题。
- 字体、字阶、字重和加载说明。
- 间距、圆角、边框、阴影和动效概览。
- 原子组件和组合组件清单。
- 布局组件和页面骨架清单。
- Showcase 清单。
- 图片、图标和内容使用规则。
- 模板要求避免的常见错误。
- 模板包大小、许可证和资产授权摘要。

这些内容用于帮助用户判断模板是否合适。V1 不提供把 token 片段或视觉形容词单独复制为复刻方案的入口。

### 12.3 真实预览

#### FR-012 内嵌交互预览

优先级：P0

详情页必须运行指定固定版本的真实 Showcase。用户可以直接操作：

- 导航和链接的演示跳转。
- Tabs、Accordion、Dropdown、Popover 和 Tooltip。
- Modal、Drawer 和其他浮层。
- 输入、选择、表单校验和错误提示。
- 搜索、筛选、排序、分页和演示数据切换。
- Loading、Empty、Error、Success 等模板状态。

要求：

- 使用 iframe 或等价隔离方式，模板样式不能污染网站外层。
- 预览运行在独立来源或严格的 iframe sandbox 中，不能读取网站 Cookie、父页面 DOM 和网站本地存储。
- 预览默认只能访问自身固定版本资产；额外网络地址必须进入发布白名单。
- 顶层跳转、弹窗、下载和剪贴板能力默认关闭，确有演示需要时逐项声明并通过安全检查。
- 运行真实模板组件，禁止用截图或不可操作的静态替代品冒充预览。
- 演示数据使用本地 fixture 或隔离状态。
- 交互不会发起真实登录、支付、邮件、订单或数据写入。
- 提供“重置演示”能力，把预览恢复到初始状态。
- 预览报错时显示明确错误状态和重试入口，网站导航仍可使用。
- 网站预览与下载模板包来自同一实现版本。

#### FR-013 内嵌视口切换

优先级：P0

内嵌预览提供以下快捷视口：

- 手机：360 x 800。
- 平板：768 x 1024。
- 桌面：1440 x 900，受详情页可用空间限制时等比容纳外框，但预览内部仍使用声明宽度布局。
- 自适应：填满当前可用预览区域。

要求：

- 使用分段控制或图标控制切换视口。
- 当前视口具有清晰选中状态和可访问名称。
- 设备外框尺寸稳定，切换时不挤压页面其他操作。
- 预览内部允许正常滚动，外部页面滚动不被无故锁定。
- 手机和窄屏下的详情页可以将预览工具栏换行，不能遮挡内容。

#### FR-014 独立浏览器预览

优先级：P0

用户点击“在新标签页打开”后，网站打开固定版本的独立预览路由。

要求：

- 独立页保留平台、框架、组件库、模板、实现版本、Showcase 和主题。
- 页面不显示模板站导航、详情侧栏和固定设备外框。
- Showcase 使用完整浏览器视口，响应式布局直接读取浏览器窗口宽高。
- 用户拖动窗口时，页面实时进入对应断点。
- 用户使用浏览器开发者工具切换设备和分辨率时，页面不覆盖浏览器提供的视口。
- 页面刷新、直接访问和分享后恢复同一固定版本预览。
- 独立页与内嵌页使用同一构建入口、组件源码和 fixture。
- 独立页也提供不干扰页面的“重置演示”入口；可收纳在简洁工具菜单中。

验收标准：

- 从详情页进入独立预览只需一次主要操作。
- 360px、768px、1024px、1440px 和浏览器自定义宽度均能正常布局。
- 固定版本 URL 在发布新版本后仍显示原内容。
- 所有声明为可交互的控件都可操作，且无真实外部副作用。

#### FR-015 Showcase 切换

优先级：P0

每个公开模板实现至少提供两个不同题材的完整 Showcase，组件实验室不计入这个数量。用户切换 Showcase 时，网站需要同步更新：

- 预览内容。
- 当前标题和简短说明。
- 独立预览地址。
- 分享地址。

不同 Showcase 应证明同一套组件、tokens 和布局规则可以适配多种业务。它们不能只是替换标题和图片的同构页面。

#### FR-016 组件实验室

优先级：P0

组件实验室集中展示模板真实组件及全部适用状态，包括：

- Default。
- Hover。
- Focus visible。
- Active 或 Pressed。
- Selected 或 Checked。
- Disabled。
- Loading。
- Invalid 或 Error。
- Empty。
- Success。
- Open 或 Expanded。

某个状态不适用于当前组件时，需要在模板状态矩阵中说明，不能无提示遗漏。组件实验室与 Showcase 使用同一组件入口。

### 12.4 复制 AI 模板迁移指令

#### FR-020 “复制给 AI”入口

优先级：P0

模板详情页必须提供清晰的“复制给 AI”主要操作。用户点击后，网站复制一段完整、可执行的模板迁移任务。

要求：

- 复制内容绑定当前固定实现版本。
- 不带版本的详情页需要先解析最新稳定版本，再生成复制内容。
- 操作成功后显示轻量反馈，并明确复制的模板 ID 和版本。
- 剪贴板权限失败时，展示可手动选择的完整文本。
- 复制文本允许保留 `{{USER_REQUEST}}` 占位符，也允许用户先输入业务需求再复制。
- 用户输入的业务需求只能进入指定内容区，不能改变下载地址、校验值、目标目录和固定步骤。

#### FR-021 迁移指令内容

优先级：P0

迁移指令必须包含以下结构化信息：

- 模板完整 ID。
- 固定实现版本和家族版本。
- 固定版本模板包 URL。
- SHA-256 校验值。
- 预期目录 `.uidevtpl/<platform>/<framework>/<library>/<template>/`。
- 目标平台和运行宿主。
- 目标框架、组件库和兼容范围。
- 业务需求填写区。

迁移指令必须要求 AI 按以下顺序执行：

1. 确认当前目录是用户要求修改的项目。
2. 阅读项目内的开发规则、技术栈、包管理器和现有改动。
3. 检查项目框架与模板实现是否兼容。
4. 使用当前环境可用的标准下载工具获取固定版本模板包。
5. 验证 SHA-256；校验失败时停止。
6. 安全解压到指定 `.uidevtpl` 目录，保留完整模板参考代码。
7. 阅读 `manifest.json`、`MIGRATION.md`、模板源码、状态规则和参考图。
8. 选择最接近业务需求的页面骨架。
9. 把需要的 tokens、字体、组件、布局和页面骨架迁移到项目现有源码目录。
10. 根据项目现有写法调整 import、Provider、路由和配置。
11. 保留模板的视觉与交互规则，替换演示内容并连接用户业务。
12. 记录源文件、目标文件、版本和依赖变更。
13. 运行目标项目已有的格式化、静态分析、类型检查、测试和构建。
14. 项目规则允许时进行桌面和手机视觉检查；需要启动开发服务器且尚未获得授权时先询问用户。
15. 报告模板版本、`.uidevtpl` 路径、迁移文件、验证结果和剩余风险。

迁移指令必须明确以下限制：

- 下载、校验或解压失败后停止，禁止根据文字描述近似重画。
- AI 不能静默覆盖用户已有文件和未提交改动。
- 项目运行时代码不能直接导入 `.uidevtpl`。
- AI 需要优先复用迁移后的模板组件，避免重新编写相似组件。
- 模板未声明的新颜色、字体、圆角和阴影需要说明原因。
- 依赖变更遵循用户项目现有包管理器和权限规则。
- 没有完成验证时，AI 不能声称模板已经应用成功。

迁移任务本身授权 AI 执行以下低风险改动：

- 新建迁移所需文件。
- 使用现有包管理器安装模板声明的兼容依赖。
- 对 Provider、全局样式、字体加载、路径别名和构建配置做可审查的增量修改。
- 运行目标项目已经存在的格式化、静态分析、类型检查、测试和构建命令。

以下情况需要暂停并取得用户明确同意：

- 覆盖用户已有文件或未提交改动。
- 删除依赖、升级主要版本或替换现有组件库。
- 切换包管理器、构建工具、路由方案或样式系统。
- 删除项目文件、改变部署方式或执行不可恢复操作。
- 启动开发服务器或需要额外系统权限的程序。

#### FR-022 迁移指令示例结构

以下示例只说明生成格式，正式内容由当前固定模板数据生成：

```text
UIDEVTPL_TEMPLATE_MIGRATION

template: web/react/heroui/quiet-grid
version: 1.0.0
family: quiet-grid@1.0.0
artifact_url: https://<uidevtpl-domain>/artifacts/web/react/heroui/quiet-grid/1.0.0/template.zip
sha256: <sha256>
expected_path: .uidevtpl/web/react/heroui/quiet-grid/

请在当前用户项目中执行以下任务：

1. 确认当前目录和项目规则。
2. 下载 artifact_url 指向的固定模板包并验证 sha256。
3. 解压到 expected_path，读取 manifest.json、MIGRATION.md、源码和参考图。
4. 检查 React、HeroUI、TypeScript、样式系统和构建工具兼容性。
5. 将所需模板代码迁移到项目现有源码目录，保留模板视觉与交互规则。
6. 运行时代码不要直接依赖 .uidevtpl。
7. 不要静默覆盖已有文件；记录迁移映射和依赖变更。
8. 完成下方业务需求，运行项目已有门禁并报告结果。
9. 下载或校验失败时停止，禁止凭文字描述近似复刻。

业务需求：
{{USER_REQUEST}}
```

### 12.5 模板包分发与本地保留

#### FR-030 固定版本模板包

优先级：P0

每个公开实现版本必须提供可直接下载的固定模板包。最低包含：

- `manifest.json`。
- `MIGRATION.md`。
- `LICENSE` 和资产授权说明。
- `checksums.json`。
- 视觉家族版本信息。
- Design Tokens 与主题文件。
- 字体声明和加载说明。
- 原子组件与组合组件源码。
- 布局组件与页面骨架源码。
- 组件状态矩阵。
- Showcase 源码或可复用示例。
- 参考截图和必要规则。

模板包不能包含：

- `node_modules`。
- 完整业务项目。
- 用户密钥或内部地址。
- 未获授权的字体、图片或图标。
- 自动启动脚本。
- 与目标模板无关的大型依赖产物。

#### FR-031 固定下载地址与校验

优先级：P0

模板包下载需要满足：

- 固定版本 URL 长期可访问。
- 同一 URL 的文件内容保持不可变。
- 页面显示的版本、文件大小和 SHA-256 与下载内容一致。
- 下载无需用户登录。
- 新版本使用新 URL，不能覆盖旧文件。
- 下载失败返回清晰状态，不能返回 HTML 错误页并伪装成压缩包。

#### FR-032 `.uidevtpl` 目录

优先级：P0

迁移指令使用以下目录约定：

```text
.uidevtpl/
  lock.json
  web/
    react/
      heroui/
        quiet-grid/
          manifest.json
          MIGRATION.md
          LICENSE
          checksums.json
          tokens/
          src/
          references/
          migration.json
```

要求：

- 平台、框架、组件库和模板各占一层，避免同名实现冲突。
- 原始模板代码长期保留，便于后续 AI 会话继续参考。
- `lock.json` 记录模板 ID、实现版本、家族版本、校验值和获取时间。
- `migration.json` 记录模板源文件与项目目标文件的对应关系。
- 项目构建不能依赖 `.uidevtpl` 中的运行时源码。
- 获取其他版本时，AI 需要先报告已有版本和差异，禁止静默替换。

#### FR-033 迁移失败处理

优先级：P0

以下情况必须停止迁移并向用户说明：

- 目标项目框架与模板框架不同。
- 组件库或关键版本明确不兼容。
- 模板包下载失败。
- SHA-256 不匹配。
- 压缩包损坏或缺少关键文件。
- 目标 `.uidevtpl` 目录存在冲突且无法安全合并。
- 迁移需要覆盖用户文件，但用户尚未同意。

失败报告至少包含失败步骤、具体原因、已经产生的文件和建议处理方式。失败后不能继续用文字猜测模板外观。

### 12.6 版本与分享

#### FR-040 固定版本

优先级：P0

模板实现使用语义化版本。已发布版本的预览、模板包、迁移指令、参考图和变更说明保持不可变。修复任何公开内容时都发布新版本。

#### FR-041 分享链接

优先级：P0

用户可以分享：

- 固定版本详情页。
- 固定版本的指定 Showcase 独立预览。

分享链接必须带完整平台、框架、组件库、模板、版本和 Showcase 信息。接收者无需登录即可访问。

#### FR-042 弃用状态

优先级：P1

发布记录的 `availability` 可以改为 `deprecated`，但固定模板包和 Manifest 保持不变，页面仍允许查看和下载，并说明弃用原因、推荐替代版本和已知风险。

#### FR-043 紧急下架

优先级：P0

安全漏洞、许可证事故、错误模板包或其他不适合继续分发的问题可以把发布记录的 `availability` 改为 `withdrawn`：

- 最新稳定版本入口和公开模板列表立即排除该版本。
- 固定版本详情保留最小身份、下架原因、下架时间和替代版本信息。
- 模板包下载可以关闭，已经发布的文件不能在原 URL 静默替换。
- 模板包内 `manifest.json` 和 SHA-256 保持不变；网站以发布记录的 `availability` 为当前公开可用性。
- 页面、sitemap、缓存和下载入口需要在一次发布操作中同步更新。
- 恢复分发时发布新实现版本；原版本的 `availability` 不改回 `active`。

## 13. 核心用户流程

### 13.1 浏览与确认模板

```text
进入模板库
  -> 按平台、框架、组件库和视觉属性筛选
  -> 打开模板详情
  -> 操作内嵌 Showcase
  -> 切换桌面、平板和手机视口
  -> 打开组件实验室检查状态
  -> 在新标签页检查完整页面和自定义分辨率
  -> 确认模板与技术栈合适
```

### 13.2 复制并迁移模板

```text
用户点击“复制给 AI”
  -> 网站生成当前固定版本的迁移指令
  -> 用户粘贴到目标项目的 AI 编程会话
  -> AI 确认项目目录和规则
  -> AI 下载并校验固定模板包
  -> AI 解压到 .uidevtpl/<platform>/<framework>/<library>/<template>/
  -> AI 读取源码、迁移说明和参考图
  -> AI 把所需代码迁移到现有项目目录
  -> AI 替换内容并接入业务
  -> AI 运行项目门禁和允许的视觉检查
  -> AI 报告迁移结果
```

### 13.3 失败流程

```text
下载、校验、兼容或文件冲突检查失败
  -> AI 停止业务代码迁移
  -> 保留或清理本次产生的半成品
  -> 向用户说明失败位置和已产生改动
  -> 用户处理问题或选择其他兼容模板
```

## 14. 模板数据要求

每个模板实现至少提供以下字段：

| 字段 | 用途 |
| --- | --- |
| `id` | 完整模板 ID |
| `version` | 固定实现版本 |
| `family.id` | 视觉家族 ID |
| `family.version` | 固定家族版本 |
| `name` | 中英文名称 |
| `summary` | 简短说明 |
| `status` | 制作状态：`draft`、`review`、`published`；公开包固定为 `published` |
| `availability` | 发布记录可用性：`active`、`deprecated`、`withdrawn` |
| `platform` | 目标平台 |
| `runtimes` | 已验证的运行宿主 |
| `framework` | 框架及支持范围 |
| `library` | 组件库及支持范围 |
| `traits` | 气质、密度、形状、对比度 |
| `themes` | 支持主题 |
| `showcases` | Showcase 列表与固定预览入口 |
| `components` | 组件和状态覆盖摘要 |
| `pageSkeletons` | 页面骨架清单 |
| `artifact` | 固定下载 URL、大小和 SHA-256 |
| `license` | 代码与资产许可证摘要 |
| `publishedAt` | 发布时间 |
| `updatedAt` | 当前版本更新时间 |

页面、迁移指令和模板包元数据必须来自同一份模板记录。手工修改其中一个展示结果后不能直接发布。

目录浏览使用独立的 `CatalogEntry`，避免分享者、分类和热度等可变信息进入不可变模板包：

| 字段 | 用途 |
| --- | --- |
| `id` | 稳定目录条目 ID |
| `templateId`、`templateVersion` | 指向通过门禁的固定模板实现 |
| `showcaseId` | 卡片缩略图与详情默认打开的 Showcase |
| `categories` | 一个或多个稳定 Category ID |
| `publisher` | 分享者 ID、显示名称、头像、类型和主页预留路径 |
| `source` | `official`、`partner` 或 `community` |
| `visibility` | `public`、`unlisted` 或 `private`；V1 公开列表只读取 `public` |
| `moderationStatus` | `pending`、`approved`、`rejected` 或 `withdrawn` |
| `metrics` | 聚合浏览量与点赞数；不得写回模板 Manifest |
| `sharedAt` | 目录条目公开时间 |

当前访问者的点赞状态由会话层单独解析，不进入可缓存的公开目录记录。账号、投稿或持久化点赞能力关闭时，字段和路由继续保留，写操作停止在能力网关。

## 15. 模板内容最低要求

V1 的每个公开模板实现至少包含：

- 完整语义 tokens。
- 字体规则与合法使用方式。
- 核心原子组件。
- 可复用组合组件。
- 页面壳和响应式布局组件。
- 营销页、应用界面、内容页三类页面骨架。
- 一个组件实验室。
- 两个不同题材的完整 Showcase。
- 适用组件状态矩阵。
- 桌面、平板和手机参考图。
- `MIGRATION.md`。
- 固定版本压缩包和 SHA-256。
- 10 个受控任务中的适用 AI 迁移验证结果。

组件实验室单独计算，不计入两个 Showcase。具体代码组织和验收规则以 [UIDevTpl 模板规范 v1](./模板规范-v1.md) 为准。

### 15.1 V1 核心组件基线

每个 V1 公开模板实现至少覆盖以下能力；实现可以按视觉家族需要继续增加组件：

| 类别 | 最低组件或能力 |
| --- | --- |
| 操作与展示 | Button、IconButton、Link、Badge、Avatar、Divider |
| 表单输入 | Input、Textarea、Select、Checkbox、Radio、Switch |
| 导航与浮层 | Tabs、Accordion、Pagination、Tooltip、Dropdown、Popover、Modal、Drawer、Toast |
| 数据与状态 | Table 或 DataList、SearchBar、FilterToolbar、EmptyState、ErrorState、Skeleton |
| 页面布局 | SiteShell、AppShell、Sidebar、PageHeader、ContentLayout、ResponsiveNavigation |

组件名称可以遵循目标生态习惯，能力、状态和交互不能无说明缺失。组件实验室需要展示每项能力的公开 API、适用状态和键盘行为。

### 15.2 V1 兼容基线

- 首个实现同时验证 Vite React SPA 和 Next.js App Router。
- React、HeroUI、TypeScript、Node.js、样式系统及关键插件在实现启动时锁定稳定兼容范围，并写入固定版本 Manifest。
- Next.js 验证需要覆盖 Provider、服务端渲染页面和交互组件客户端边界。
- npm 和 pnpm 分别完成至少一次干净项目安装、构建和迁移验证。
- 兼容信息只使用“已验证”“理论兼容”和“不支持”三种结论；公开页面不能把未经验证的组合标记为已支持。

### 15.3 许可证基线

- UIDevTpl 自有模板代码使用 MIT License，允许商业使用、修改和迁移到业务项目。
- UIDevTpl 名称、Logo 和品牌资产不包含在模板代码授权中。
- 字体、图片、图标和插图逐项写入 `ASSETS-LICENSE.md`，记录来源、许可证、署名和再分发范围。
- 不允许随模板包再分发的资产不能进入下载包；模板需要提供可合法获取的替代方式和缺失资产降级结果。
- 网站代码、第三方组件库和模板代码分别记录许可证，避免用户误解授权范围。

### 15.4 AI 迁移验收矩阵

- 首批固定为 10 个受控迁移任务，Codex 和 Claude Code 各执行 5 个。
- 任务需要覆盖 Vite、Next.js、npm、pnpm、全新项目、已有业务代码和未提交改动等情况。
- 业务题材至少包含三个模板没有预制的未知场景，不能只迁移 Showcase 同类页面。
- 成功任务必须完成下载、SHA-256 校验、`.uidevtpl` 落盘、业务源码迁移、`migration.json`、项目门禁和结果报告。
- 至少 9 个任务成功才达到数量门槛；静默覆盖、跳过校验、伪造验证结果和越权执行属于单项发布阻断问题。
- 至少两名人工评审者独立评分；风格延续平均分不低于 4/5，单项不得低于 3/5，评分差达到 2 分时需要复核并记录结论。

## 16. 非功能需求

### 16.1 性能

- 模板库首屏只加载可见缩略图。
- 真实预览按路由和模板实现懒加载。
- 首页不能下载全部模板运行时代码。
- 图片使用 WebP 或 AVIF，并声明宽高比。
- 公开页面 p75 LCP 目标不高于 2.5 秒。
- 公开页面 p75 INP 目标不高于 200 毫秒。
- 公开页面 p75 CLS 目标不高于 0.1。

### 16.2 响应式

- 网站自身在 360px、768px、1024px 和 1440px 宽度可用。
- 工具栏、筛选器和模板 ID 长文本可以换行或收纳。
- 内嵌预览外框具有稳定尺寸约束。
- 页面不存在阻断操作的横向溢出和遮挡。
- 独立预览尊重浏览器真实视口。

### 16.3 无障碍

- 目标符合 WCAG 2.2 AA。
- 所有主要操作支持键盘。
- Focus visible 清晰可见。
- 图标按钮具有可访问名称和 Tooltip。
- 颜色不能成为唯一状态信号。
- 动效支持 `prefers-reduced-motion`。
- 普通文本对比度至少 4.5:1。

### 16.4 安全

- 只运行受信任仓库发布的预览代码。
- 预览与主站使用独立来源或等价隔离，权限按最小集合开放。
- 预览网络访问默认关闭，固定版本资产和经过审核的演示资源使用白名单。
- 预览交互不能执行真实外部业务操作。
- 模板包使用固定版本和 SHA-256。
- 下载文件名、路径和压缩包条目需要经过发布检查。
- 用户输入的业务需求不能注入或改写迁移指令的系统字段。
- 网站不收集用户项目代码、文件路径、环境变量和 AI 对话。
- 前端不暴露部署密钥。

### 16.5 浏览器与分享

- 支持发布时 Chrome、Edge 和 Firefox 最新及前一个稳定主版本。
- 支持发布时 Safari 和 iOS Safari 最新及前一个稳定主版本。
- 固定版本详情和独立预览具备可分享元信息。
- 分享卡片优先使用真实 Showcase 截图。
- 每个稳定模板版本拥有独立可访问 URL。

### 16.6 Cloudflare Workers

- 网站、固定版本数据、预览入口和模板包可通过 Cloudflare Workers 环境稳定访问。
- 固定版本模板包和参考图支持长期缓存。
- 最新稳定版本入口允许较短缓存并正确更新。
- 构建产物符合部署时 Cloudflare 的有效限制。

### 16.7 匿名产品统计

- 记录模板列表曝光、模板详情访问、独立预览打开、迁移指令复制、模板包下载结果和预览错误。
- 统计事件只包含模板 ID、固定版本、入口、结果和必要的错误分类。
- 不采集用户填写的业务需求、项目内容、文件路径、AI 对话、账号标识和可用于跨站跟踪的浏览器指纹。
- 网站公开说明统计用途和保留期限；启用第三方统计服务时同步记录数据去向。

### 16.8 SEO

- 模板库、使用说明、已发布模板详情和固定版本页面提供可抓取 HTML、唯一标题、摘要和 canonical URL。
- 不带版本的最新稳定详情页把 canonical 指向当前解析出的固定版本页。
- 站点提供 sitemap 和 robots.txt，sitemap 只收录公开可访问内容。
- 独立预览页面使用 `noindex`，避免与模板详情形成重复内容。
- 分享元信息包含固定模板版本、真实 Showcase 图片和稳定图片尺寸。
- `availability=deprecated` 的页面保留搜索入口并说明替代版本；`availability=withdrawn` 的页面使用 `noindex` 且不进入 sitemap。

## 17. V1 内容范围

### 17.1 首个模板

- 工作 ID：`web/react/heroui/quiet-grid`。
- 实现版本：从 `1.0.0` 开始。
- 视觉家族：`quiet-grid@1.0.0`。
- 平台：Web。
- 运行宿主：Browser。
- 框架：React。
- 组件库：HeroUI。
- 主题：V1 只发布 Light。
- 网站语言：中文与 English 可切换；模板名称、分类、筛选和操作文案提供对应语言，偏好在刷新后恢复。
- Showcase：AI 项目工作台、内容出版或知识杂志。
- 页面骨架：营销页、应用界面、内容页。

工作名和具体视觉方向需要在模板设计阶段确认后锁定。锁定前不能对外发布稳定版本。

### 17.2 公开 V1 的模板数量

功能闭环先用 `quiet-grid` 完成内部验收。公开 V1 至少发布两个明显不同的视觉家族，每个家族至少提供一个完整 Web 实现。第二个家族不能通过浅层换色生成，也必须独立通过全部发布门禁。

## 18. 整体验收场景

### AC-001 浏览并检查模板

1. 用户进入模板库。
2. 用户选择 React 和 HeroUI。
3. 用户打开 `web/react/heroui/quiet-grid@1.0.0`。
4. 用户在内嵌预览操作导航、表单、Tabs 和弹窗。
5. 用户切换手机、平板和桌面视口。
6. 页面无阻断级溢出、遮挡和无响应控件。

### AC-002 独立浏览器预览

1. 用户选择一个 Showcase。
2. 用户点击“在新标签页打开”。
3. 新页面显示相同固定版本和 Showcase。
4. 用户调整浏览器窗口宽度，模板按断点响应。
5. 用户使用浏览器设备模拟切换分辨率，页面使用模拟视口正常布局。
6. 刷新和分享链接后仍恢复相同预览。

### AC-003 复制迁移指令

1. 用户点击“复制给 AI”。
2. 复制内容包含固定模板 ID、实现版本、家族版本、下载 URL、SHA-256 和目标目录。
3. 用户把内容粘贴到目标 React 项目的 AI 编程会话。
4. AI 下载并校验模板包。
5. AI 将模板解压到 `.uidevtpl/web/react/heroui/quiet-grid/`。
6. AI 读取真实源码并迁移所需文件到项目现有目录。
7. AI 完成业务适配并报告验证结果。

### AC-004 下载失败时停止

1. 模板包 URL 不可访问或 SHA-256 不匹配。
2. AI 停止迁移。
3. AI 说明失败原因和已经产生的文件。
4. AI 不根据模板介绍文字重新绘制近似页面。

### AC-005 框架不兼容时停止

1. 用户在 Vue 项目中使用 `web/react/heroui/quiet-grid`。
2. AI 读取 manifest 后识别框架不兼容。
3. AI 在迁移业务文件前停止。
4. AI 建议用户选择已发布的 Vue 实现；没有可用实现时如实说明。

### AC-006 固定版本可复现

1. 用户访问固定版本详情页和独立预览。
2. 页面版本、预览内容、下载包和迁移指令一致。
3. 发布 `1.1.0` 后，`1.0.0` 的 URL、模板包和预览保持不变。

### AC-007 多场景风格延续

1. 同一模板提供两个不同题材的 Showcase。
2. 两个页面使用相同 tokens、组件和布局规则。
3. AI 使用模板完成第三个未知题材页面。
4. 人工评审确认色彩、字体、密度、形状、组件状态和布局节奏保持一致。

### AC-008 受控迁移验收

1. Codex 和 Claude Code 各执行 5 个受控任务。
2. 任务矩阵覆盖 Vite、Next.js、npm、pnpm、新项目和已有项目。
3. 至少 9 个任务完成全部成功条件。
4. 所有任务均没有静默覆盖、跳过校验、伪造验证结果和越权执行。
5. 至少两名评审者完成独立视觉评分并保存原始记录。

### AC-009 紧急下架

1. 维护者将一个已发布固定版本记录的 `availability` 改为 `withdrawn`。
2. 最新稳定入口和公开列表不再选择该版本。
3. 固定版本页显示原因、时间和替代版本。
4. 下载关闭后不会返回旧包、替换包或伪装成压缩包的 HTML。
5. 其他已发布固定版本继续正常访问。

## 19. 待确认事项

以下事项仍需在对应实现开始前确认，不改变已经锁定的 V1 产品范围：

1. UIDevTpl 域名、商标和品牌视觉。
2. `quiet-grid` 的正式展示名称和最终视觉方案。
3. 第二个视觉家族的 ID、视觉方向和两个 Showcase 题材。
4. 模板包固定下载域名、存储方案和长期保留年限。
5. 实现启动时 React、HeroUI、Node.js、样式系统和构建工具的具体稳定版本范围。
6. 匿名统计的自建或第三方实现、数据保留期限和公开隐私说明。
7. 首批两名以上人工评审者及评分复核负责人。
8. 账号、社区投稿和持久化点赞采用的身份服务、审核策略与开放里程碑。

MCP 已明确不进入 V1，无需列为 V1 待确认项。

## 20. V1 发布定义

V1 同时满足以下条件才可以公开发布：

- 本文所有 P0 功能通过验收。
- 模板库、固定版本详情、内嵌预览和独立预览可访问。
- 首页分类预览、分类总览和单分类浏览可访问，入口与 URL 一致。
- 网站 Light / Dark 与中文 / English 切换可用并能恢复偏好。
- 模板卡片展示分享者头像、名称、浏览量和点赞数，心形状态可即时切换。
- 登录、注册和分享模板的预留入口受能力开关控制，关闭时不产生伪请求。
- 内嵌与独立预览运行真实组件并支持声明的交互。
- 手机、平板、桌面和自定义浏览器视口检查通过。
- “复制给 AI”生成固定版本迁移指令。
- 模板包可以从固定 URL 下载且 SHA-256 正确。
- AI 能将模板包保留到 `.uidevtpl` 并迁移到真实项目。
- 下载、校验和框架不兼容场景能正确停止。
- 首个 React / HeroUI 模板符合独立模板规范。
- 第二个视觉家族至少有一个完整 Web 实现并通过相同门禁。
- 10 个受控迁移任务至少成功 9 个，且没有单项发布阻断问题。
- 至少三个未知业务题材的人工风格评分达到标准。
- 网站格式化、静态分析、类型检查、测试和生产构建全部通过。
- Cloudflare Workers 生产环境可访问。
- 发布记录、许可证、参考截图和回退方案齐全。

V1 完成证据必须包含可访问地址、固定模板版本、构建结果、预览截图、模板包校验结果和 AI 迁移验证记录。
