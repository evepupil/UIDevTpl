# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

UIDevTpl 首要服务使用 Codex、Claude Code、Cursor、Windsurf、GitHub Copilot 等 AI 编程工具开发产品的独立开发者和前端开发者。他们具备基本开发能力，但缺少一套可以直接检查、复用并让 AI 持续遵循的完整视觉系统。

用户通常已经有一个目标项目，希望减少审美沟通、截图往返和后续页面逐渐走样的问题。他们需要在几分钟内找到合适模板，检查真实界面和技术兼容性，然后把固定版本迁移指令交给当前项目中的 AI。

## Product Purpose

UIDevTpl 提供经过人工打磨、可真实预览、可下载并可迁移的 UI 开发模板。每套模板包含真实源码、Design Tokens、字体与资产规则、组件、布局、页面骨架、交互状态和 Showcase。

一次成功的首次访问需要完成三件事：用户找到合适模板，确认真实预览和兼容信息，并复制绑定固定版本的迁移指令。AI 随后下载并校验模板包，把原始参考源保存到 `.uidevtpl/`，再按目标项目规则迁移需要的界面系统。

## Positioning

UIDevTpl 依靠固定版本的完整模板源码和可执行迁移流程保持复刻稳定性。模板详情、真实预览、下载产物、校验信息和迁移指令绑定同一个版本；简单视觉形容词和孤立的风格提示词不承担模板复刻职责。

## Operating Context

- 用户先在浏览器中搜索、筛选并操作真实模板，检查页面、组件状态、常用视口和技术栈。
- 用户把迁移指令粘贴到目标项目中的 AI 编程会话，由 AI 阅读项目规则、下载并校验固定模板包。
- 原始模板长期保存在目标项目的 `.uidevtpl/<platform>/<framework>/<library>/<template>/` 目录，业务代码进入项目原有源码结构。
- 主站负责发现、判断和迁移入口；独立 Preview Project 负责运行固定版本 Showcase；模板包和校验产物通过受控发布链路提供。

## Capabilities and Constraints

- V1 支持匿名浏览、筛选、真实预览、独立预览、迁移指令复制和固定模板包下载，不要求登录。
- V1 暂不支持用户上传模板、投稿接收、审核后台、账号系统、个人中心和跨设备同步。目录模型、稳定路由和能力网关保留未来扩展位置，关闭状态不产生伪写入请求。
- V1 首个公开实现是 Web / React / shadcn/ui；其他框架、组件库和运行宿主通过版本化目录模型扩展。
- 主站与预览站分别构建和部署。主站运行时不得导入模板运行时代码，模板构建失败不能阻断主站。
- 最新入口必须解析到一个固定版本；详情、预览、下载、SHA-256、Registry 派生产物和迁移指令保持同一身份。
- 网站支持中文 / English 与 Light / Dark；网站主题和模板自身主题能力分别声明。
- 网站不能读取或修改用户本地项目，也不能自动安装依赖、启动开发服务器或远程执行用户项目。
- 匿名统计只记录模板身份、固定版本、入口和结果，不收集业务需求、项目内容、文件路径、AI 对话或跨站浏览器指纹。

## Brand Commitments

- 产品名称使用 `UIDevTpl`，仓库与命令使用 `uidevtpl`，项目参考目录使用 `.uidevtpl/`。
- UIDevTpl 名称、Logo 和品牌资产不随 MIT 模板源码授权。
- 对外文案保持直接、具体和可验证，不虚构客户、采用量、评价、性能数据或已经开放的能力。

## Evidence on Hand

- `packages/templates/web/react/shadcn/blackline-saas/` 包含首个完整模板的源码、Manifest、迁移说明、组件实验室和 Showcase。
- `packages/catalog/` 与 `packages/schema/` 提供当前版本化目录、固定路由、筛选和迁移身份契约及其单元测试。
- `apps/preview/` 提供固定版本预览深链；`apps/web/` 已验证目录、详情和迁移入口的现有行为。
- 当前没有可以公开使用的客户评价、采用规模、商业价格或迁移成功率数据，后续界面不得自行补造。

## Product Principles

1. 用真实源码和真实交互证明模板质量。
2. 先让用户看清效果与兼容性，再进入迁移。
3. 所有交付面共享同一个固定版本身份。
4. 保持匿名主路径短而完整，未来能力通过明确边界扩展。
5. 尊重目标项目结构、现有改动和验证规则，失败时停止并如实报告。

## Accessibility & Inclusion

主站和预览入口需要支持语义化结构、键盘操作、清晰焦点、可读对比度、缩放和不同尺寸下的容器自适应。状态不能只依靠颜色表达；加载、空状态、错误、成功和长文本都需要可理解结果。当前没有单独确认的无障碍认证目标。
